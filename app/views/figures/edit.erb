<h1>Edit Historic Figure</h1>
<form method="POST" action="/figures/<%=@figure.id%>">
<input type="hidden" id="hidden" name="_method" value="PATCH"/>

<label for="figure_name">Name: </label>
<input type="text" id="figure_name" name="figure[name]" value="<%=@figure.name%>"/>

<br>

<h3>Select and/or Create Titles</h3>
  <% Title.all.each do |title| %>
    <input id="title_<%= title.id %>" type="checkbox" name="figure[title_ids][]" value="<%= title.id %>" <%='checked' if @figure.titles.include?(title) %>><%=title.name%></input>
  <% end %>

<br>

<input type="text" id="new_title" name="title[name]" placeholder="New Title"/>

<br><br>

<h3>Select and/or Create Landmarks</h3>
  <% Landmark.all.each do |landmark| %>
    <input id="landmark_<%= landmark.id %>" type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" <%='checked' if @figure.landmarks.include?(landmark) %>><%=landmark.name%></input>
  <% end %>
<br>

<input type="text" id="new_landmark" name="landmark[name]" placeholder="New Landmark"/>

<br><br>

<input type="submit" id="Edit Figure" value="Edit Figure"/>

</form>
